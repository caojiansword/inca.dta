<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE generatorConfiguration
         PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
         "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
 
 <generatorConfiguration>
 		<!-- jdbc连接的mysql或者oracle的连接包jar，最好是配置为绝对路径 -->
     <context id="my" targetRuntime="MyBatis3">
       <plugin type="org.mybatis.generator.plugins.EqualsHashCodePlugin"></plugin>
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin"></plugin>
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin"></plugin>
         <commentGenerator>
            <!-- 这个元素用来去除指定生成的注释中是否包含生成的日期 false:表示保护 -->
            <!-- 如果生成日期，会造成即使修改一个字段，整个实体类所有属性都会发生变化，不利于版本控制，所以设置为true -->
             <property name="suppressAllComments" value="true"/>
              <property name="suppressDate" value="true" />
         </commentGenerator>
 		<!-- 配置数据源信息，多数据源需要配置多份文件，对应需要修改数据源信息，可以使不同主机的数据库，也可以是同一数据库服务器上的不同数据库名 -->
         <jdbcConnection driverClass="com.mysql.jdbc.Driver" connectionURL="jdbc:mysql://10.0.0.123:3306/dta" userId="root" password="123456">    
        </jdbcConnection>   
         <javaTypeResolver>
            <!-- This property is used to specify whether MyBatis Generator should
                force the use of java.math.BigDecimal for DECIMAL and NUMERIC fields, -->
            <property name="forceBigDecimals" value="false" />
        </javaTypeResolver>
 		<!-- 配置生成的model对象也就是javabean，对应数据库表属性的javabean生成到的包路径和绝对代码路径，只需要配置到工程的/src/msunyardn/java就可以了，具体的路径由包路径指定。
 			由于多数据源，所以我在model后面加了一层以示区分不同数据源的信息 -->
         <javaModelGenerator targetPackage="com.inca.entity.gen"
                             targetProject="target">
             <property name="enableSubPackages" value="true"/>
             <property name="trimStrings" value="true"/>
         </javaModelGenerator>
 		<!-- mapper文件生成路劲和包名 -->
         <sqlMapGenerator targetPackage="com.inca.mapping"
                          targetProject="tartet">
             <property name="enableSubPackages" value="true"/>
         </sqlMapGenerator>
 		<!-- dao层接口文件 -->
         <javaClientGenerator targetPackage="com.inca.mapper"
                              targetProject="target" type="XMLMAPPER">
             <property name="enableSubPackages" value="true"/>
         </javaClientGenerator>
 		<!-- 数据库里面的表名信息和配置，多张表需要配置多条信息 -->
 		<table tableName="pub_customer" domainObjectName="Customer"
                enableCountByExample="true" enableUpdateByExample="true"
                enableDeleteByExample="true" enableSelectByExample="true"
                selectByExampleQueryId="true">
         </table> 
     </context>
 </generatorConfiguration>